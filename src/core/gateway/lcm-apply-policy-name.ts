/** Content-free policy identity used by LCM apply receipts and activation records. */
export const LCM_APPLY_POLICY = "lcm-context-optimize" as const;
export type LcmApplyPolicyName = typeof LCM_APPLY_POLICY;
